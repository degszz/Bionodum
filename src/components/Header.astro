---
const navLinks = [
  { label: 'Inicio', href: '#inicio' },
  {
    label: 'Productos',
    href: '#productos',
    children: [
      { label: 'Aditivos', href: '#aditivos' },
      { label: 'Ingredientes', href: '#ingredientes' },
      { label: 'Premix', href: '#premix' },
    ],
  },
  {
    label: 'Servicios',
    href: '#servicios',
    children: [
      { label: 'Servicios a Campo', href: '#servicios-a-campo' },
      { label: 'Data Analytics', href: '#data-analytics' },
      { label: 'Análisis de Calidad', href: '#analisis-calidad' },
    ],
  },
  { label: 'Acerca de', href: '#acerca-de' },
  { label: 'Contacto', href: '#contacto' },
];
---

<header
  id="header"
  class="fixed top-0 left-0 right-0 z-50 transition-all duration-500"
>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-20">
      <!-- Logo -->
      <a href="#inicio" class="flex items-center gap-3 group" aria-label="Bionodum - Inicio">
        <img
          src="/images/logo.avif"
          alt="Bionodum Logo"
          width="64"
          height="64"
          class="h-16 w-auto object-contain transition-transform duration-300 group-hover:scale-105"
          loading="eager"
        />
        <span id="header-title" class="font-display font-700 text-xl text-white hidden sm:block tracking-tight transition-colors duration-500">
          Bionodum
        </span>
      </a>

      <!-- Desktop Nav -->
      <nav class="hidden lg:flex items-center gap-1" aria-label="Navegación principal">
        {navLinks.map((link) => (
          <div class="relative group">
            <a
              href={link.href}
              class="header-link flex items-center gap-1 px-4 py-2 text-sm font-medium text-white/90 rounded-lg hover:text-white hover:bg-white/10 transition-all duration-300"
            >
              {link.label}
              {link.children && (
                <svg class="w-3.5 h-3.5 transition-transform duration-200 group-hover:rotate-180" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                </svg>
              )}
            </a>
            {link.children && (
              <div class="absolute top-full left-0 mt-1 w-52 bg-white rounded-xl shadow-lg border border-gray-100 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 translate-y-1 group-hover:translate-y-0 z-50">
                {link.children.map((child) => (
                  <a
                    href={child.href}
                    class="block px-4 py-2.5 text-sm text-gray-600 hover:text-brand-500 hover:bg-brand-50 first:rounded-t-xl last:rounded-b-xl transition-colors duration-150"
                  >
                    {child.label}
                  </a>
                ))}
              </div>
            )}
          </div>
        ))}
      </nav>

      <!-- CTA Button + Mobile trigger -->
      <div class="flex items-center gap-3">
        <a
          id="header-cta"
          href="#contacto"
          class="hidden lg:inline-flex items-center px-5 py-2.5 bg-white/15 hover:bg-white/25 border border-white/30 text-white text-sm font-semibold rounded-full transition-all duration-300 backdrop-blur-sm hover:-translate-y-0.5"
        >
          Contactanos
        </a>

        <!-- Mobile hamburger -->
        <button
          id="mobile-menu-btn"
          class="lg:hidden p-2 rounded-lg text-white hover:text-white hover:bg-white/10 transition-colors"
          aria-label="Abrir menú"
          aria-expanded="false"
          aria-controls="mobile-menu"
        >
          <span id="hamburger-icon">
            <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
          </span>
          <span id="close-icon" class="hidden">
            <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </span>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile menu — adapts to transparent/solid header -->
  <div
    id="mobile-menu"
    class="lg:hidden hidden transition-colors duration-300"
    aria-label="Menú móvil"
  >
    <div id="mobile-menu-inner" class="max-w-7xl mx-auto px-4 py-4 space-y-1 bg-black/60 backdrop-blur-xl">
      {navLinks.map((link) => (
        <div>
          <a
            href={link.href}
            class="mobile-link block px-4 py-3 text-sm font-medium text-white/90 hover:text-white hover:bg-white/10 rounded-lg transition-colors"
          >
            {link.label}
          </a>
          {link.children && (
            <div class="ml-4 mt-1 space-y-1">
              {link.children.map((child) => (
                <a
                  href={child.href}
                  class="mobile-link block px-4 py-2 text-sm text-white/70 hover:text-white hover:bg-white/10 rounded-lg transition-colors"
                >
                  {child.label}
                </a>
              ))}
            </div>
          )}
        </div>
      ))}
      <div class="pt-3 border-t border-white/20">
        <a
          href="#contacto"
          class="block w-full text-center px-5 py-3 bg-brand-400 hover:bg-brand-500 text-white text-sm font-semibold rounded-full transition-colors"
        >
          Contactanos
        </a>
      </div>
    </div>
  </div>
</header>

<!-- NO spacer — hero is fullscreen under transparent header -->

<script>
  const btn = document.getElementById('mobile-menu-btn');
  const menu = document.getElementById('mobile-menu');
  const hamburger = document.getElementById('hamburger-icon');
  const closeIcon = document.getElementById('close-icon');

  btn?.addEventListener('click', () => {
    const isOpen = !menu?.classList.contains('hidden');
    menu?.classList.toggle('hidden');
    hamburger?.classList.toggle('hidden');
    closeIcon?.classList.toggle('hidden');
    btn.setAttribute('aria-expanded', String(!isOpen));
  });

  // Close mobile menu on link click
  menu?.querySelectorAll('a').forEach((link) => {
    link.addEventListener('click', () => {
      menu.classList.add('hidden');
      hamburger?.classList.remove('hidden');
      closeIcon?.classList.add('hidden');
      btn?.setAttribute('aria-expanded', 'false');
    });
  });

  // ---- Scroll: transparent → solid with blur (no border) ----
  const header = document.getElementById('header');
  const headerTitle = document.getElementById('header-title');
  const headerCta = document.getElementById('header-cta');
  const headerLinks = document.querySelectorAll('.header-link');
  const mobileBtn = document.getElementById('mobile-menu-btn');
  const mobileMenuInner = document.getElementById('mobile-menu-inner');
  const mobileLinks = document.querySelectorAll('.mobile-link');

  function updateHeader() {
    const scrolled = window.scrollY > 60;

    if (scrolled) {
      // Solid mode — white blur, NO border
      header?.classList.add('bg-white/90', 'backdrop-blur-xl', 'shadow-lg');
      headerTitle?.classList.remove('text-white');
      headerTitle?.classList.add('text-gray-800');
      headerLinks.forEach((l) => {
        l.classList.remove('text-white/90', 'hover:text-white', 'hover:bg-white/10');
        l.classList.add('text-gray-600', 'hover:text-brand-500', 'hover:bg-brand-50');
      });
      headerCta?.classList.remove('bg-white/15', 'hover:bg-white/25', 'border-white/30', 'backdrop-blur-sm');
      headerCta?.classList.add('bg-brand-400', 'hover:bg-brand-500', 'border-transparent', 'shadow-sm');
      mobileBtn?.classList.remove('text-white', 'hover:text-white', 'hover:bg-white/10');
      mobileBtn?.classList.add('text-gray-600', 'hover:text-brand-500', 'hover:bg-brand-50');
      // Mobile menu solid
      mobileMenuInner?.classList.remove('bg-black/60');
      mobileMenuInner?.classList.add('bg-white/95');
      mobileLinks.forEach((l) => {
        l.classList.remove('text-white/90', 'text-white/70', 'hover:text-white', 'hover:bg-white/10');
        l.classList.add('text-gray-700', 'hover:text-brand-500', 'hover:bg-brand-50');
      });
    } else {
      // Transparent mode — no background, no border, no shadow
      header?.classList.remove('bg-white/90', 'backdrop-blur-xl', 'shadow-lg');
      headerTitle?.classList.remove('text-gray-800');
      headerTitle?.classList.add('text-white');
      headerLinks.forEach((l) => {
        l.classList.remove('text-gray-600', 'hover:text-brand-500', 'hover:bg-brand-50');
        l.classList.add('text-white/90', 'hover:text-white', 'hover:bg-white/10');
      });
      headerCta?.classList.remove('bg-brand-400', 'hover:bg-brand-500', 'border-transparent', 'shadow-sm');
      headerCta?.classList.add('bg-white/15', 'hover:bg-white/25', 'border-white/30', 'backdrop-blur-sm');
      mobileBtn?.classList.remove('text-gray-600', 'hover:text-brand-500', 'hover:bg-brand-50');
      mobileBtn?.classList.add('text-white', 'hover:text-white', 'hover:bg-white/10');
      // Mobile menu transparent
      mobileMenuInner?.classList.remove('bg-white/95');
      mobileMenuInner?.classList.add('bg-black/60');
      mobileLinks.forEach((l) => {
        l.classList.remove('text-gray-700', 'hover:text-brand-500', 'hover:bg-brand-50');
        l.classList.add('text-white/90', 'hover:text-white', 'hover:bg-white/10');
      });
    }
  }

  updateHeader();
  window.addEventListener('scroll', updateHeader, { passive: true });
</script>
